server {
    listen 80;

    location / {
        root /var/www/events-ui;
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /404.html;
    location = /404.html {
        root /var/www/events-ui;
        internal;
    }
}